---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: obliquo/dind
    tag: latest

inputs:
  - name: nanogo-repo

run:
  path: docker-compose
  args: ["run", "--rm", "tests"]
  dir: nanogo-repo/ci
